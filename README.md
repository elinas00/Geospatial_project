# Geospatial_project
A full workflow with geoai and a pretrained DL-model, including preparing ortophotos and tiles, fine-tune a pretrained model, inference and compute accuracy. Project also includes post-processing of predicted car data to calculate shortest path between two random cars. 
